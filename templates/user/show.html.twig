{% extends 'base.html.twig' %}

{% block title %}Utilisateur
{% endblock %}

{% block body %}
	<div class="container-fluid">
		<h2>
			Fiche Utilisateur
		</h2>
		<div class="fade-in">
			<div class="row">
				<div class="col-lg-12">
					<div class="card">
						<div class="card-header">
							<i class="fa fa-align-justify"></i>
							Fiche utilisateur
						</div>
						<div class="card-body">
							<table class="table table-reponsive-sm">
								<thead>
									<tr>
										<th>Email</th>
										<th>Roles</th>
										<th>Password</th>
									</tr>
								</thead>
								<tbody>
									<tr>
										<td>{{ user.email }}</td>
										<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
										<td>{{ user.password }}</td>
									</tr>
								</tbody>
							</table>
							<a class="btn btn-sm btn-primary mt-3" type="submit" href="{{ path('user_edit', {'id': user.id}) }}">Editer l'utilisateur</a>
							<a href="{{ path('user_index') }}" class="btn btn-sm btn-info mt-3">Retour à la liste</a>
							{{ include('user/_delete_form.html.twig') }}
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
