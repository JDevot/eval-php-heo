{% extends 'base.html.twig' %}

{% block title %}Edition ticket
{% endblock %}

{% block body %}
	<div class="container-fluid">
		<h1>Modification du ticket</h1>
		<div class="row">
			<div class="col-md-6">
				<div class="card">
					<div class="card-header">
						<strong>Formulaire de modification du ticket</strong>
					</div>
					<div class="card-body">
						{{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}
						{{ include('ticket/_delete_form.html.twig') }}
					</div>
				</div>
			</div>
		</div>
	</div>
</div>{% endblock %}
