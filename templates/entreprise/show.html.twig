{% extends 'base.html.twig' %}

{% block title %}Entreprise
{% endblock %}

{% block body %}
	<div class="container-fluid">
		<h2>
			Fiche Entreprise
		</h2>
		<div class="fade-in">
			<div class="row">
				<div class="col-lg-12">
					<div class="card">
						<div class="card-header">
							<i class="fa fa-align-justify"></i>
							Fiche Entreprise
						</div>
						<div class="card-body">
							<table class="table table-reponsive-sm">
								<thead>
									<tr>
										<th>Nom</th>
										<th>Adresse</th>
									</tr>
								</thead>
								<tbody>
									<tr>
										<td>{{ entreprise.name }}</td>
										<td>{{ entreprise.adress }}</td>
									</tr>
								</tbody>
							</table>
							<div class="row">
								<div class="col-1">
									<a href="{{ path('entreprise_edit', {'id': entreprise.id}) }}" href="{{ path('entreprise_edit', {'id': entreprise.id}) }}" class="btn btn-sm btn-primary mt-3">Editer</a>
								</div>
								<div class="col-2">
									<a href="{{ path('entreprise_index') }}" class="btn btn-sm btn-info mt-3">
										<i class="cib-nextdoor"></i></a>
									</div>
									<div class="col-1">
										{{ include('entreprise/_delete_form.html.twig') }}
									</div>
								</div>
							</div>
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
